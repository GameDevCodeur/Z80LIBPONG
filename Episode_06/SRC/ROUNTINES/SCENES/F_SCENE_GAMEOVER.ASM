
; ===============================================================
;  GAME OVER - Initialisation
; ===============================================================

F_SCENE_GAMEOVER_INITIALISATION

    ; Eteindre scène
    CALL F_ETEINDRE_ENCRES
	
    ; Nettoyer scène
    mCLEAR_BUFFER SCR_VRAM_BASE		; AFHL!

    ; Afficher titre PONG
    LD   HL, MENU_ZX0
    LD   DE, SCR_VRAM_BASE
    CALL F_ZX0_DECOMPRESS

    ; Affiche "PRESS M TO MENU"
    mAFFICHER_CHAR_4x8 FNT.CHAR_P, GAME_OVER_VRAM_TEXT_MENU+00
    mAFFICHER_CHAR_4x8 FNT.CHAR_R, GAME_OVER_VRAM_TEXT_MENU+02
    mAFFICHER_CHAR_4x8 FNT.CHAR_E, GAME_OVER_VRAM_TEXT_MENU+04
    mAFFICHER_CHAR_4x8 FNT.CHAR_S, GAME_OVER_VRAM_TEXT_MENU+06
    mAFFICHER_CHAR_4x8 FNT.CHAR_S, GAME_OVER_VRAM_TEXT_MENU+08
    mAFFICHER_CHAR_4x8 FNT.CHAR_M, GAME_OVER_VRAM_TEXT_MENU+12
    mAFFICHER_CHAR_4x8 FNT.CHAR_T, GAME_OVER_VRAM_TEXT_MENU+16
    mAFFICHER_CHAR_4x8 FNT.CHAR_O, GAME_OVER_VRAM_TEXT_MENU+18
    mAFFICHER_CHAR_4x8 FNT.CHAR_M, GAME_OVER_VRAM_TEXT_MENU+22
    mAFFICHER_CHAR_4x8 FNT.CHAR_E, GAME_OVER_VRAM_TEXT_MENU+24
    mAFFICHER_CHAR_4x8 FNT.CHAR_N, GAME_OVER_VRAM_TEXT_MENU+26
    mAFFICHER_CHAR_4x8 FNT.CHAR_U, GAME_OVER_VRAM_TEXT_MENU+28

    ; Affiche "PRESS N TO NEW GAME"
    mAFFICHER_CHAR_4x8 FNT.CHAR_P, GAME_OVER_VRAM_TEXT_RESTART+00
    mAFFICHER_CHAR_4x8 FNT.CHAR_R, GAME_OVER_VRAM_TEXT_RESTART+02
    mAFFICHER_CHAR_4x8 FNT.CHAR_E, GAME_OVER_VRAM_TEXT_RESTART+04
    mAFFICHER_CHAR_4x8 FNT.CHAR_S, GAME_OVER_VRAM_TEXT_RESTART+06
    mAFFICHER_CHAR_4x8 FNT.CHAR_S, GAME_OVER_VRAM_TEXT_RESTART+08
    mAFFICHER_CHAR_4x8 FNT.CHAR_N, GAME_OVER_VRAM_TEXT_RESTART+12
    mAFFICHER_CHAR_4x8 FNT.CHAR_T, GAME_OVER_VRAM_TEXT_RESTART+16
    mAFFICHER_CHAR_4x8 FNT.CHAR_O, GAME_OVER_VRAM_TEXT_RESTART+18
    mAFFICHER_CHAR_4x8 FNT.CHAR_N, GAME_OVER_VRAM_TEXT_RESTART+22
    mAFFICHER_CHAR_4x8 FNT.CHAR_E, GAME_OVER_VRAM_TEXT_RESTART+24
    mAFFICHER_CHAR_4x8 FNT.CHAR_W, GAME_OVER_VRAM_TEXT_RESTART+26
    mAFFICHER_CHAR_4x8 FNT.CHAR_G, GAME_OVER_VRAM_TEXT_RESTART+30
    mAFFICHER_CHAR_4x8 FNT.CHAR_A, GAME_OVER_VRAM_TEXT_RESTART+32
    mAFFICHER_CHAR_4x8 FNT.CHAR_M, GAME_OVER_VRAM_TEXT_RESTART+34
    mAFFICHER_CHAR_4x8 FNT.CHAR_E, GAME_OVER_VRAM_TEXT_RESTART+36
	
    ; Allumer scène
    LD   HL, MENU_PAL
    LD   BC, GA_PORT
    LD    A, GAME_OVER_PALETTE_SIZE
    mGA_SET_PALETTE (VOID)

    RET

; ===============================================================
;  GAME OVER - Boucle principale
; ===============================================================

F_SCENE_GAMEOVER

    mKEY_M_REQUESTED (VOID)
    LD   B, ID_SCENE_MENU
    JR   Z, .load
    
    mKEY_N_REQUESTED (VOID)
    RET  NZ
    LD   B, ID_SCENE_JEU

.load
    JP   F_SCENE_CHARGER
