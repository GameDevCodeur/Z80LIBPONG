
INCLUDE "..\mGET_PADDLE_LIMITE.INC"

F_PADDLE_MISE_A_JOUR

	mKEY_A_REQUESTED (VOID)	; !AF
	JR  Z, F_PADDLE_HAUT	
	mKEY_Q_REQUESTED (VOID)	; !AF
	RET NZ

F_PADDLE_BAS
	
	mGET_PADDLE_LIMITE ENT_PADDLE_LIMITE_BAS : RET Z
	
	INC A

	LD  (ENT_PADDLE.CY), A
	LD  HL, (ENT_PADDLE.VRAM_XY)
	LD  (ENT_PADDLE.OLD_XY), HL
	
	mSET_NEXTLINE_HL (VOID)
	JR  F_PADDLE_STORE

F_PADDLE_HAUT
	
	mGET_PADDLE_LIMITE ENT_PADDLE_LIMITE_HAUT : RET Z
	
	DEC A

	LD  (ENT_PADDLE.CY), A
	LD  HL, (ENT_PADDLE.VRAM_XY)
	LD  (ENT_PADDLE.OLD_XY), HL

	mSET_PREVIOUSLINE_HL (VOID)

F_PADDLE_STORE

	LD  (ENT_PADDLE.VRAM_XY), HL
	RET
	